#!/usr/bin/env bash
set -eu

<% if link('uaa_db').p('release_level_backup') %>
  /var/vcap/jobs/bpm/bin/bpm run bbr-uaadb \
    -p restore \
    -v "${BBR_ARTIFACT_DIRECTORY%/}:writable" \
    -e BBR_ARTIFACT_DIRECTORY="$BBR_ARTIFACT_DIRECTORY"
<% end %>
